{
  "name": "gitlab-plugin-codex",
  "version": "0.2.0",
  "private": true,
  "description": "Production-focused Codex plugin workspace for GitLab workflows backed by glab.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "validate": "node ./scripts/validate.js",
    "mcp:list-tools": "node ./scripts/invoke-mcp-tool.js list-tools",
    "mcp:invoke": "node ./scripts/invoke-mcp-tool.js",
    "test": "node --test",
    "check": "npm run validate && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Razin-Tailor/gitlab-plugin-codex.git"
  },
  "keywords": [
    "codex",
    "plugin",
    "gitlab",
    "glab"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  }
}
